# ملف كودكس لمشروع برندة

**آخر تحديث:** 2026-07-13 21:25 +03:00  
**المسار الفعلي:** `E:\branda-platform`  
**الغرض من هذا الملف:** خريطة تشغيل وتوثيق حالة يعتمد عليها كودكس قبل أي تعديل لاحق.

## 1) تعريف المشروع والمسار

- المشروع الموجود فعليا على القرص هو `E:\branda-platform`.
- تم التحقق من وجود المشروع ومن وجود `package.json` و `app` و `components` و `lib` و `supabase`.
- هذه الجولة جولة تأسيس وتوثيق ونسخ احتياطي فقط.
- لم يتم تعديل منطق التطبيق أو الواجهات أو قاعدة البيانات في هذه الجولة.

## 2) قواعد العمل غير القابلة للكسر

- لا يتم التخمين في المسارات أو الملفات أو النتائج.
- لا يتم تنفيذ `git add .` أو `commit` أو `push` إلا بطلب صريح.
- لا يتم حذف أو نقل أو إعادة تسمية ملفات إلا بعد تقرير وموافقة.
- أي تعديل حساس يجب أن يسبق بتنبيه يتضمن الملف، المشكلة، سبب الحساسية، التأثير، المخاطر، البدائل، والتوصية.
- لا يتم إنشاء Migration جديد إلا بأمر صريح.
- لا يتم تعديل قاعدة البيانات أو ربطها في مهام التطبيق إلا بعد مراجعة المتطلبات الفعلية.
- لا يتم تعديل ملف Migration رقم `001` إلا عند وجود متطلب فعلي واضح وبعد تحديث نفس الملف المناسب، لا إنشاء ملف جديد.
- في كل مهمة مستقبلية يجب مراجعة احتياجات الجداول والأعمدة والعلاقات والفهارس و RLS والسياسات و Storage buckets وسياسات التخزين والدوال و triggers، ثم تحديث نفس ملف `001` عند الحاجة.

## 3) سياسة العربية والترميز

- أي نص عربي داخل ملفات JSX أو TS أو SQL أو MD يجب أن يكون عربيا سليما ومحفوظا بترميز UTF-8.
- ممنوع حفظ نص عربي مشوه أو رموز غير مفهومة.
- بعد أي تعديل يحتوي على العربية يجب تشغيل فحص سلامة النص:

```powershell
npm run check:text
```

- يحتوي `AGENTS.md` على قاعدة إلزامية بعدم تسليم العمل إذا ظهرت أنماط mojibake المحظورة في `scripts/check-text-integrity.mjs`.

## 4) التقنية ومدير الحزم والسكربتات

- مدير الحزم الفعلي: npm، بسبب وجود `package-lock.json`.
- نسخة الحزم المثبتة حسب `npm ls --depth=0`:
  - `next@16.2.6`
  - `react@19.2.4`
  - `react-dom@19.2.4`
- `package.json` يستخدم نطاقات `latest` لعدة حزم، لذلك يجب الاعتماد على `package-lock.json` عند التحقق من النسخ المثبتة.
- سكربتات `package.json`:

```json
{
  "dev": "next dev --turbopack",
  "check:text": "node scripts/check-text-integrity.mjs",
  "build": "next build",
  "security:source": "node scripts/security-source-gate.mjs",
  "start": "next start",
  "lint": "next lint",
  "db:reset": "supabase db reset",
  "db:test": "supabase test db",
  "db:push": "supabase db push",
  "db:start": "supabase start",
  "audit:security": "node scripts/barndaksa-security-audit.mjs",
  "verify:source": "node scripts/barndaksa-source-verify.mjs"
}
```

## 5) خريطة المجلدات والملفات الأساسية

- `app`: مسارات App Router وصفحات التطبيق، ويحتوي على نحو 179 ملفا.
- `components`: مكونات الواجهات، ويحتوي على نحو 221 ملفا.
- `lib`: منطق الخدمات والبيانات والتكاملات، ويحتوي على نحو 181 ملفا.
- `supabase`: إعدادات Supabase والهجرات والاختبارات والبذور، ويحتوي على نحو 109 ملفات.
- `docs`: وثائق وتقارير وتسليمات المشروع.
- `public`: أصول عامة وصور وعلامات تجارية وأصول ألعاب.
- `scripts`: سكربتات الفحص والتحقق والأمان والتوليد.
- `types`: تعريفات TypeScript إضافية.
- `package.json` و `package-lock.json`: تعريف الحزم وقفل الإصدارات.
- `next.config.ts` و `proxy.ts` و `tsconfig.json` و `eslint.config.mjs`: ملفات إعداد أساسية.

## 6) نقاط الدخول الأساسية

- `app/page.tsx`: الصفحة الرئيسية.
- `app/layout.tsx`: التخطيط العام.
- `app/globals.css`: الأنماط العامة.
- `app/c/[slug]/page.tsx`: صفحة عامة مرتبطة بالعلامة.
- `app/dashboard`: لوحة العلامة.
- `app/admin`: لوحة الإدارة.
- `app/actions`: Server Actions متعددة.
- `app/api`: مسارات API.

## 7) نظام Feature Gates

تم التحقق من وجود ملفات نظام Feature Gates التالية فعليا:

- `lib/platform/feature-registry.ts`
- `lib/platform/feature-access.ts`
- `lib/platform/public-feature-access.ts`
- `components/dashboard/feature-gate.tsx`

ويجب مراجعتها قبل أي تعديل يتعلق بالباقات أو الاشتراكات أو إظهار المزايا.

## 8) ملفات الباقات والاشتراكات والنمو

تم التحقق من وجود الملفات التالية فعليا:

- `app/admin/plans/page.tsx`
- `app/dashboard/subscription/page.tsx`
- `app/dashboard/growth/page.tsx`
- `components/dashboard/pages/growth-page.tsx`
- `lib/data/growth.ts`

أي تعديل في الاشتراكات أو الباقات أو مركز النمو يجب أن يراجع هذه الملفات مع نظام Feature Gates.

## 9) المناطق الحساسة

- `supabase/migrations`: هجرات قاعدة البيانات.
- `supabase/config.toml`: إعدادات Supabase.
- `lib/supabase`: عملاء Supabase والصلاحيات.
- `lib/platform`: الباقات والمزايا والاشتراكات.
- `app/actions`: عمليات الخادم المؤثرة على البيانات.
- `app/admin`: واجهات وصلاحيات الإدارة.
- `lib/storage`: الوصول للملفات والتخزين.
- ملفات البيئة مثل `.env.local` و `.env.example`.
- `scripts/security-source-gate.mjs` و `scripts/check-text-integrity.mjs`.

هذه المناطق لا تعدل إلا بعد فحص مباشر وتقرير واضح عند وجود حساسية.

## 10) سياسة Migration 001

تم العثور داخل `supabase/migrations` على ملفين يبدأان بالرقم `001`:

```text
supabase/migrations/001_barndaksa_production_schema.sql
supabase/migrations/001_branda_production_schema.sql
```

بسبب وجود أكثر من ملف `001` لا يوجد ملف واحد واضح يمكن اعتماده وحده في هذه الجولة. يجب تسجيل هذا الغموض وعدم التعامل مع Migration 001 حتى يصدر قرار واضح عن الملف المقصود أو قاعدة اختيار موثقة.

بصمات SHA256 المسجلة للفحص فقط:

```text
supabase/migrations/001_barndaksa_production_schema.sql
C17C39ECDC93A7C27B489E4BEFE4A15430E64D57CBD477BD96C295A5D5650BDA

supabase/migrations/001_branda_production_schema.sql
08A2F5979C14381C8D82F1193DA7BB9A63D00437A258325DE4C261C0097E0224
```

لم يتم تعديل أي Migration في هذه الجولة.

## 11) ملفات البيئة والأسرار

تم رصد ملفات البيئة التالية بالاسم فقط دون قراءة محتواها:

```text
.env.example
.env.local
```

يجب عدم طباعة أسرار أو مفاتيح في التقارير، ويجب استبعاد ملفات البيئة من أي ZIP مخصص للمراجعة.

## 12) سياسة التصميم الاحترافي

- أي واجهة مستقبلية يجب أن تكون عالية الجودة ومتناسبة مع هوية المشروع.
- لا يتم تغيير تجربة المستخدم أو المنطق الحالي بلا ضرورة موثقة.
- يجب الحفاظ على الاتساق مع بنية المشروع الحالية ومكوناته.
- يجب مراجعة ملفات التصميم والهوية قبل تغيير الألوان أو التخطيط أو تجربة لوحة الإدارة أو لوحة العلامة.

## 13) حالة Git الأولية

- الفرع عند الفحص: `main`
- آخر Commit عند الفحص: `91a96ed Gate public games access by brand feature`
- كانت هناك ملفات غير متتبعة قبل هذه الجولة، منها وثائق تدقيق وأصول عامة ومجلد `tmp`.
- لم يتم تنفيذ `git add` أو `commit` أو `push`.

## 14) سجل الحالة لهذه الجولة

- تم فحص المسار الفعلي للمشروع.
- تم قراءة `AGENTS.md`.
- تم قراءة فهرس وثائق Next.js المحلية في `node_modules/next/dist/docs/index.md` التزاما بتعليمات المشروع قبل أي عمل كتابي.
- تم فحص `package.json` والسكربتات ومدير الحزم.
- تم التحقق من وجود Supabase ومجلد `migrations`.
- تم رصد وجود أكثر من ملف `001` داخل `supabase/migrations`.
- تم إنشاء هذا الملف كتحديث توثيقي فقط.
- لا تحتوي هذه الجولة على تعديل في منطق التطبيق أو الواجهات أو قاعدة البيانات.

## 15) طريقة التقرير بعد كل مهمة

أي تقرير لاحق يجب أن يذكر بوضوح:

- المسار الفعلي للمشروع.
- الفرع وآخر Commit.
- حالة Git قبل وبعد المهمة.
- الملفات التي أنشئت أو عدلت فعليا.
- الفحوصات التي شغلت ونتائجها الفعلية.
- وضع Migration 001 وهل تغير أم لا.
- أي غموض أو تحذيرات أو مناطق حساسة.
- تأكيد صريح عند عدم تعديل منطق التطبيق أو الواجهات أو قاعدة البيانات.

## 16) نتائج فحوصات هذه الجولة

- `npm run check:text`: نجح، ونتيجته `Text integrity check passed`.
- `npx tsc --noEmit --pretty false --incremental false`: نجح بدون مخرجات خطأ.
- `npm run lint`: فشل بدون تعديل ملفات، لأن سكربت `next lint` أعاد الخطأ: `Invalid project directory provided, no such directory: E:\branda-platform\lint`.
- لم يتم تشغيل `build`.
- لم يتم تشغيل أوامر Supabase.

## 17) حالة النسخة المضغوطة

- تم إنشاء ZIP للمراجعة على القرص `E:`.
- يجب اعتماد المسار والحجم و SHA256 المذكورة في تقرير كودكس النهائي لهذه الجولة، لأنها تحسب بعد حفظ هذا الملف داخل الأرشيف.
- يجب ألا يحتوي ZIP على ملفات البيئة أو `node_modules` أو `.next` أو `.git` أو `.vercel` أو `tmp` أو ملفات ZIP سابقة أو `tsconfig.tsbuildinfo`.
- يجب أن يحتوي ZIP النهائي على `كودكس.md` بالاسم العربي السليم.

## 18) تأكيد نطاق التغيير

- الملف التطبيقي الوحيد الذي أنشئ في هذه الجولة هو ملف التوثيق `كودكس.md`.
- لا يوجد تعديل في منطق التطبيق.
- لا يوجد تعديل في الواجهات.
- لا يوجد تعديل في قاعدة البيانات أو ملفات migrations.
