# BARNDAKSA_SUBSCRIPTION_STATUS_ENUM_FIX_NODE

Run from project root:

```bash
node BARNDAKSA_SUBSCRIPTION_STATUS_ENUM_FIX_NODE/apply.cjs
npm run build
npm run dev
```

Fixes invalid enum value `paid` in queries against `public.subscription_status`.
